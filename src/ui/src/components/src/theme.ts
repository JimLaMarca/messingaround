import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#24a885',
        },
        secondary: {
            light: '#0066ff',
            main: '#46c42a',
            contrastText: '#ffcc00',
        },
}})